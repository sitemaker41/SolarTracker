const CONFIG = { maxQuestions: 3, storageKey: 'solarBot_questions' }; let questionCount = 0; function loadQuestionCount() { const saved = localStorage.getItem(CONFIG.storageKey); if (saved) { questionCount = parseInt(saved) || 0; } updateQuestionCounter(); } function saveQuestionCount() { localStorage.setItem(CONFIG.storageKey, questionCount.toString()); } function updateQuestionCounter() { const counter = document.getElementById('questionCounter'); if (counter) { const remaining = Math.max(0, CONFIG.maxQuestions - questionCount); counter.textContent = remaining; } } function addMessage(type, text) { const chatContainer = document.getElementById('chatMessages'); if (!chatContainer) return; const messageDiv = document.createElement('div'); messageDiv.className = `chat-message ${type}`; const icon = document.createElement('span'); icon.className = 'chat-icon'; icon.textContent = type === 'user' ? '👤' : '🤖'; const textSpan = document.createElement('span'); textSpan.className = 'chat-text'; textSpan.innerHTML = text.replace(/\n/g, '<br>'); messageDiv.appendChild(icon); messageDiv.appendChild(textSpan); chatContainer.appendChild(messageDiv); chatContainer.scrollTop = chatContainer.scrollHeight; } function showWelcomeMessage() { const welcomeText = `👋 سلام! من SolarBot 1.0 هستم، دستیار هوشمند انرژی خورشیدی. از من هر سوالی درباره انرژی خورشیدی، پنل‌های خورشیدی، ردیاب‌های دو محوره و تک‌محوره، سنسور LDR، سروو موتور، آردوینو و ... بپرسید. 📌 توجه: در هر جلسه می‌توانید ۳ سوال بپرسید.`; addMessage('bot', welcomeText); } function processQuestion(userInput) { if (!userInput || userInput.trim() === '') return; if (questionCount >= CONFIG.maxQuestions) { addMessage('bot', '⚠️ شما به حداکثر سوالات مجاز (۳ سوال) در این جلسه رسیده‌اید. برای پرسش سوال بیشتر، صفحه را رفرش کنید.'); return; } addMessage('user', userInput); questionCount++; saveQuestionCount(); updateQuestionCounter(); const result = findBestMatch(userInput); setTimeout(() => { let response = result.answer; if (result.score > 0 && result.topic) { } addMessage('bot', response); }, 300); } function initSolarBot() { loadQuestionCount(); showWelcomeMessage(); const form = document.getElementById('chatForm'); const input = document.getElementById('chatInput'); const sendBtn = document.getElementById('chatSendBtn'); if (form) { form.addEventListener('submit', (e) => { e.preventDefault(); const text = input.value.trim(); if (text) { processQuestion(text); input.value = ''; } }); } if (sendBtn && input) { sendBtn.addEventListener('click', () => { const text = input.value.trim(); if (text) { processQuestion(text); input.value = ''; } }); input.addEventListener('keypress', (e) => { if (e.key === 'Enter') { e.preventDefault(); const text = input.value.trim(); if (text) { processQuestion(text); input.value = ''; } } }); } const clearBtn = document.getElementById('clearChatBtn'); if (clearBtn) { clearBtn.addEventListener('click', () => { document.getElementById('chatMessages').innerHTML = ''; questionCount = 0; localStorage.removeItem(CONFIG.storageKey); updateQuestionCounter(); showWelcomeMessage(); }); } } document.addEventListener('DOMContentLoaded', initSolarBot);
